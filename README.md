🕰️ Clock Simulator + NFT Metadata Generator
A delightfully retro‑crap web app that simulates an analog clock with digital rain and sub‑second precision — and lets you mint that exact moment as an ERC‑721 compatible metadata JSON with an embedded image snapshot.

Live demo: open index.html in any modern browser.
No server, no build, no dependencies – just pure HTML, CSS & vanilla JS.

